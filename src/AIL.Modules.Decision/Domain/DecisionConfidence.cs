namespace AIL.Modules.Decision.Domain;

/// <summary>Deterministic confidence tier derived from evaluation scores in Phase 2.</summary>
public enum DecisionConfidence
{
    Low = 0,
    Medium = 1,
    High = 2
}
